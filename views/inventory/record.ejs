<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>

<body class="bg-yellow-50 min-h-screen py-8 px-4">
  <main class="w-[min(100%,_32rem)] mx-auto px-12 py-10 text-red-500 font-[Nunito] rounded-2xl">
    <%-
      include('../partials/navheader',
        {
          title: "Record Stock",
          subtitle: "Fill in the form below to add a new entry to this item's stock history.",
          destination: `/inventory/${itemId}/show`
        })
    %>

    <form method="post" action="<%= `/inventory/${itemId}/record` %> ">
      <div class="grid grid-cols-2 gap-3 mb-4">
        <div class="grid">
          <label class="font-semibold">Operation</label>
          <select class="w-full p-2 mt-2 text-gray-700 bg-gray-200 border border-gray-200 rounded-md" name="operation">
            <option value="add">Add stock</option>
            <option value="remove" selected>Remove stock</option>
          </select>
        </div>
        <div class="grid">
          <label class="font-semibold">Amount</label>
          <input type="number" class="w-full px-4 py-2 mt-2 text-gray-700 bg-white border border-gray-200 rounded-md focus:border-blue-400 focus:ring-blue-300 focus:ring-opacity-40 dark:focus:border-blue-300 focus:outline-none focus:ring" min="1" value="1" name="amount" />
        </div>
      </div>
      <div class="flex justify-end">
        <button type="submit" class="bg-red-600 text-white font-medium font-[Roboto] leading-5 px-6 py-2 transition-colors duration-200 rounded-md hover:bg-red-500">
          Save
        </button>
      </div>
    </form>
  </main>
</body>

</html>