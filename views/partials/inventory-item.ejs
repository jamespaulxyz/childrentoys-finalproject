<li class="bg-orange-400 transition-colors text-white hover:bg-orange-300 p-px gap-4 rounded-xl" onclick="toggleItemSelection('<%= inventoryItem.id %>')">
  <img src="<%= inventoryItem.image[0] %>" alt="Item image" class="h-64 w-full object-cover rounded-t-xl" />
  <div>
    <p class="text-xl font-bold text-center px-3 mt-2 mb-1 whitespace-nowrap overflow-hidden text-ellipsis">
      <%= inventoryItem.name %>
    </p>
    <div class="flex justify-between px-3 text-lg">
      <p>₱
        <%= inventoryItem.price %>
      </p>
      <p>
        <%= inventoryItem.currentStock %> in stock
      </p>
    </div>
    <div class="px-3 flex justify-end mt-2 mb-3">
      <a onclick="event.stopPropagation()" href="<%= `/inventory/${inventoryItem.id}/show` %> " class="font-bold hover:underline underline-offset-4">View details</a>
    </div>
  </div>
</li>